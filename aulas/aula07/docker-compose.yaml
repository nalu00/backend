services:
  db:
    image: mongodb/mongodb-community-server:7.0-ubuntu2204
    container_name: meumongo
    ports:
      - 24017:27017
    volumes:
      - ./:/mongo/data
    tty: true
    stdin_open: true